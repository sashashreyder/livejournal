specVersion: "v1beta"
serviceId: "personal-diary"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "personal-diary-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
